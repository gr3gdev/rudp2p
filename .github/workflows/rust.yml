name: Rust tests

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: cargo build --verbose
      - name: Prepare tests
        run: docker-compose -f tests/compose.yml up -d
      - name: Run tests with feature "sqlite"
        run: cargo test --features="sqlite" --no-default-features
      - name: Run tests with feature "mysql"
        run: cargo test --features="mysql" --no-default-features
      - name: Clear tests
        run: docker-compose -f tests/compose.yml down
      - name: Push reports
        if: github.ref_name != 'master'
        run: |
          git config --local user.name "GitHub Action [bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config advice.addIgnoredFile false
          git add -f mysql.md sqlite.md
          git commit -m "Add cucumber reports"
          git push origin ${{ github.head_ref }}
